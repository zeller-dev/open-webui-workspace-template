# Keep model warm in VRAM — avoids reload delay on the next request
OLLAMA_KEEP_ALIVE=30m

# More memory-efficient attention, no quality loss
OLLAMA_FLASH_ATTENTION=1

# Halves KV cache memory (near-lossless), frees VRAM for bigger models/context
OLLAMA_KV_CACHE_TYPE=q8_0

TZ=UTC