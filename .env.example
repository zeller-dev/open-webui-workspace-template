# Ports
OLLAMA_PORT=11434
WEBUI_PORT=3000

# Default model to keep loaded (used by scripts, not compose itself)
DEFAULT_MODEL=qwen3:14b

# Open WebUI auth (set to false to require login screen)
WEBUI_AUTH=true

# Timezone for container logs/timestamps
TZ=UTC
