{
  "name": "llm-stack",
  "version": "1.0.0",
  "private": true,
  "description": "Ollama + Open WebUI as two independent projects sharing one Docker network",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "up": "npm --prefix ollama run up && npm --prefix open-webui run up",
    "down": "npm --prefix open-webui run down && npm --prefix ollama run down",
    "reset": "npm --prefix open-webui run reset && npm --prefix ollama run reset",
    "logs:ollama": "npm --prefix ollama run logs",
    "logs:webui": "npm --prefix open-webui run logs",
    "list": "npm --prefix ollama run list",
    "auto-import": "npm --prefix ollama run auto-import",
    "search": "npm --prefix ollama run search --",
    "list-files": "npm --prefix ollama run list-files --",
    "fetch": "npm --prefix ollama run fetch --"
  }
}